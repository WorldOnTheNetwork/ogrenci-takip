#!/bin/sh
# Basit gradle wrapper - Actions'ta çalışması için
if [ -f "gradle/wrapper/gradle-wrapper.jar" ]; then
    exec java -classpath gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain "$@"
else
    exec gradle "$@"
fi
